/*
  FIFO - first in first out
  Queue q = new Queue();
  q.enqueue(x); //adds x to the back of the queue
  x = q.dequeue(); //remove and return the item at the head if the queue
  q.empty(); //is it empty?
  q.front(); //returns front/head value


 */
